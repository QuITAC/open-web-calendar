# Queerref Fork of OpenWebCalender

## Run Install

Clone Repo to host

```console
docker compose up .
```

Ensure `default-specification.yml` is correct and your proxy is set up correctly for routing th ssl trafic to the calendar webapp.
